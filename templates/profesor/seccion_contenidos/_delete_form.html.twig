<form class="col-2" method="post" action="{{ path('seccion_contenidos_delete', {'id': seccion_contenido.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ seccion_contenido.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>
