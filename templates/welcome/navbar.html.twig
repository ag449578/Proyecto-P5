

    <nav class="navbar navbar-expand-lg navbar-light bg-light">

        <div class="container-fluid">

            <a class="navbar-brand" href="{{path('welcome')}}">SGCA</a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">

            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                {% for key, link in links %}
                    <li class="nav-item">
                        <a class="nav-link"
                            aria-current="page" 
                            href="{{path(link)}}">{{key}}</a>
                    </li>
                {% endfor %}
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="#">Materiales</a>
                </li>
            </ul>

            <ul class="d-flex navbar-nav">

            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            {{ app.user.getNombUsuario }}
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="{{path('usuario_detail', {id: app.user.getId})}}">Perfil</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_logout')}} ">Salir</a></li>
                    </ul>
                </li>
            {% else %}
                <li class="nav-item"><a href="{{path('app_login')}}" class="nav-link">Entrar</a></li>
            </ul>
            {% endif %}

            </div>
        </div>
    </nav>